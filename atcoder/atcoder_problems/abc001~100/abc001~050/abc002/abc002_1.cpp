#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
#define ll long long

int main() {
    ll X, Y; 
    cin >> X >> Y;
    if (X > Y) cout << X << endl;
    else cout << Y << endl;
    return 0;
}